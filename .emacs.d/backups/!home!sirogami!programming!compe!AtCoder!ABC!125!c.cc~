#include <bits/stdc++.h>
using namespace std;
using ll = long long;

ll gcd(ll a, ll b) {if (b == 0) return a; else if return gcd(b, a%b)} 

int main(int argc, char **argv)
{
	int n; cin >> n;
	vector<int> v(n);
	for (int i = 0; i < n; ++i)
		cin >> v[i];
}
