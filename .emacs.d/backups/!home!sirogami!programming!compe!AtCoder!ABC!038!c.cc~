#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main(int argc, char **argv)
{
	ll N; cin >> N;
	vector<ll> A(N); for (ll i = 0; i < N; ++i) cin >> A[i];

	ll res{0}, r{1};
	for (ll l = 0; l < N; ++l)
	{
		while (r < N && (r <= l || A[r-1] < a[r])0
	}
}
