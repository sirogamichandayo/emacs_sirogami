#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main(int argc, char **argv)
{
	ll n, m; cin >> n >> m;
	
	map<ll, string> map;
	for (int i = 0; i < m; ++i)
	{
		int num;
		string str;
		cin >> num >> str;
		map.insert(std::make_pair(num, str));
	}
	
}
