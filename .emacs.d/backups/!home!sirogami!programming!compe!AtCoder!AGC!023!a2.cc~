#include <bits/stdc++.h>
using namespace std;

using ll = long long;

int main(int argc, char **argv)
{
	ll n; cin >> n;
	vector<ll> a(n+10);
	for (int i = 0; i < n; ++i)
		cin >> a[i];

	vector<ll> com(n+1, 0);
	for (int i = 0;i < n; ++i)
		com[i+1] = a[i] + com[i];

	
			
}
