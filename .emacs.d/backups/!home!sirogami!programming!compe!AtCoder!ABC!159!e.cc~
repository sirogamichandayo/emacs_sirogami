#include <bits/stdc++.h>
using namespace std;

using ll = long long;
using P = pair<ll, ll>;

const ll MOD = 1e9+7;
const ll INF = 1ll<<60;

#define FOR(i,a,b) for (ll i=(a);i<(ll)(b);++i)
#define REP(i,n) FOR(i,0,n)
#define ALL(v) (v).begin(),(v).end()

ll cnt[15][1005];
char grid[15][1005];
ll H, W, K;

int main(int argc, char **argv)
{
	cin >> H >> W >> K;
	REP(h, H)REP(w, W)
		cin >> grid[h][w];

	ll res{INF};
	for (ll bits = 0; bits < (1ll<<(H-1)); ++bits)
	{
		REP(h, H-1)
		{
			
		}
	}
}
