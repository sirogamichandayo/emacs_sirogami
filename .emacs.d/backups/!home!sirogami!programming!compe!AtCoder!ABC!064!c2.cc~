#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main(int argc, char **argv)
{
	int n; cin >> n;

	set<int> s;
	int over_num{0};
	for (int i = 0; i < n; ++i)
	{
		int a; cin >> a;
		if (a > 3200)
			++over_num;
		else
		{
			s.insert(i/400);
		}
	}

	
}
