#include <bits/stdc++.h>
using namespace std;
using ll = long long;

ll GCD(ll a, ll b)
{
	if (b == 0) return a;
	else return GCD(b, a%b);
}

ll n;
vector<ll> a(100000);

int main(int argc, char **argv)
{
	cin >> n;
	for (int i = 0; i < n; ++i)
		cin >> a[i];

	ll ans = 0;
	for (int i = 0; i < n; ++i)
		
}
