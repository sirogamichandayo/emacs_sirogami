#include <bits/stdc++.h>
using namespace std;
using ll = long long;
using pll = pair<ll, ll>;

int main(int argc, char **argv)
{
	ll N; cin >> N;
	vector<pll> X(N);
	for (ll i = 0; i < N; ++i)
	{
		ll x, l; cin >> x >> l;
		X[i] = pll(x, l);
	}

	
}
