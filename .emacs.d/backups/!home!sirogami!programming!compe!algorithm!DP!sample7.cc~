#include <bits/stdc++.h>
using namespace std;
using ll = long long;
ll INF = 1ll << 60;

template<class T> bool chmin(T &a, T b){if(a>b){a=b;return true;}return false;}
template<class T> bool chmix(T &a, T b){if(a<b){a=b;return true;}return false;}

ll n, w;
ll table[110][100010];

int main(int argc, char **argv)
{
	cin >> n >> w;
	vector<ll> w(n), v(n);
	for (ll i = 0; i < n; ++i) cin >> w[i] >> v[i];

	
}
