#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main(int argc, char **argv)
{
	int n; cin >> n;
	vector<int> p(n), q(n);
	vector<int> v(n);
	for (int i = 1; i <= n; ++i)
		cin >> v[i];

	int count = 1;
	int a{0}, b{0};
	while (next_permutation(v.begin(), v.end()))
	{
		
	}
}
