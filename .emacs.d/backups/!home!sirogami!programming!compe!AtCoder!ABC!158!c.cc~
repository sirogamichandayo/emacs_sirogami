#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main(int argc, char **argv)
{
	ll A, B;
	cin >> A >> B;

	ll a{-1}, b{-1};
	ll res{0};
	while (a <= A && b <= B)
	{
		a = floor(res * 0.08);
		b = floor(res * 0.1);
		++res;
	}
	std::cout << res << std::endl;
}
