#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main(int argc, char **argv)
{
	string str; cin >> str;
	int N = str.size();
	int c0{0}, c1{0};
	for (int i = 0; i < N; ++i)
	{
		if (str[i] == '0')
			++c0;
		else
			++c1;
	}
	
}
