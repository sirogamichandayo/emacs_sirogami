#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main(int argc, char **argv)
{
	ll n; cin >> n;
	vector<ll> a(n+10);
	for (int i = 1; i <= n; ++i)
		cin >> a[i];

	ll min = LLONG_MAX;
	while (true)
	{
		
	}
}
