#include <iostream>

#include "../src/recognition/state_main.h"
#include "../src/recognition/screen_shot.h"
#include "../src/log/log.h"
#include "../src/tools/debug/debug.h"

#include "opencv2/opencv.hpp"
#include "opencv2/highgui.hpp"

namespace pic_ = pic::hd;

int main(int argc, char **argv)
{
	// // Initialize
	ScreenShot scr(pic_::X, pic_::Y, pic_::WIDTH, pic_::HEIGHT);
	State env(scr, pic_::pic_rect_list);

	// // ScreenShot
	// env.step();

	// // Get img
	// cv::Mat img;
	// env.getImg(&img, true);
	// debug::showForDebug(img, 3000);
}
