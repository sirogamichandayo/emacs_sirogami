#include <bits/stdc++.h>
using namespace std;
using ll = long long;
using Graph = vector<vector<ll>>;

int main(int argc, char **argv)
{
	ll n, m;
	cin >> n >> m;
	Graph G;

	for (int i = 0; i < m; ++i)
	{
		int a, b;
		cin >> a >> b;
		
	}
}
