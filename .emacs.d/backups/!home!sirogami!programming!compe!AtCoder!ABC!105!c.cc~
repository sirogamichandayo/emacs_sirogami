#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main(int argc, char **argv)
{
	ll N; cin >> N;

	vector<ll> v;
	v.reserve(40);
	ll i{0};
	for (; N > 0; ++i)
	{
		v.push_back(N % 2);
		N /= 2;
	}
	reverse(v.begin(), v.end());
	vector<ll> res(40);
	
}
