#include <bits/stdc++.h>
using namespace std;
using ll = long long;

bool isPrime(ll num)
{
	if (num <= 2) return false;
	for (ll i = 2; i*i <= num; ++i)
	{
		if (num % i == 0)
			return true;
	}
	return false;
}

int main(int argc, char **argv)
{
	std::cout << isPrime(23)<< std::endl;
}

