#include <bits/stdc++.h>
using namespace std;
using ll = long long;

ll dp[110][4][4];

int main(int argc, char **argv)
{
	ll N, K; cin >> N >> K;
	vector<ll> P(N+1, 0);
	for (ll i = 0; i < K; ++i)
	{
		ll a, b; cin >> a >> b;
		P[--a] = b;
	}

	
}
