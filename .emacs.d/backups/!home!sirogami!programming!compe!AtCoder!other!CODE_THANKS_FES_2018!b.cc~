#include <bits/stdc++.h>
using namespace std;
using ll = long long;

inline ll factorial(ll num)
{
	if (num == 1) return 1;
	return num * factorial(num-1);
}

int main(int argc, char **argv)
{
	int n, m;
	cin >> n >> m;
	vector<int> r(m);
	for (int i = 0; i < m; ++i) cin >> r[i];
	
	double per = `;

	
}
