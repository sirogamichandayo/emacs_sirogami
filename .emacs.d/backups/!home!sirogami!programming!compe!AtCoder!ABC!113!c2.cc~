#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main(int argc, char **argv)
{
	ll N, M; cin >> N >> M;
	vector<ll> p(M), y(M);
	for (ll i = 0; i < M; ++i) cin >> p[i] >> y[i];

	vector<vector<ll>> vals(N);
	for (int i = 0; i < M; ++i) vals[p[i]].push_back(y[i]);
	
}
