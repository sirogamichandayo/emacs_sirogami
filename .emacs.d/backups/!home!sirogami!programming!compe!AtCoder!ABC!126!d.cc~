///////////////////?????????????????????????????????????????????????????????????????????
#include <bits/stdc++.h>
using namespace std;
using Graph = vector<pair<int, pair<int, int>>>;

int n;
Graph G;

vector<int> color(100010, false);
bool dfs(const Graph &g, int v, int cur, int d)
{
	auto next = g[v].second;
	int next_v = next.first;
	int next_d = next.second;

	
	
}

int main(int argc, char **argv)
{
	cin >> n;
	G.reserve(1000010);

	for (int i = 0; i < n - 1; ++i)
	{
		int tmp1, tmp2, tmp3;
		cin >> tmp1 >> tmp2 >> tmp3;
		G.push_back(pair<int, pair<int, int>>(tmp1, pair<int, int>(tmp2, tmp3)));
	}

	
}

