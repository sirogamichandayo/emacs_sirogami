#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main(int argc, char **argv)
{
	ll N; cin >> N;
	vector<int> a(N), b(N, -1);
	for (ll i = 0; i < N; ++i) cin >> a[i];

	for (ll i = N/2; i < N; ++i)
		b[i] = a[i];

	
	
}
