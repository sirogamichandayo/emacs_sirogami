#include <bits/stdc++.h>
using namespace std;

int n;
vector<int> a;

int main(int argc, char **argv)
{
	cin >> n;
	for (int i = 1; i <= n; ++i)
		cin >> a[i];

	int target = 1;

	while (true)
	{
		for (int i = 1; i <= n; ++i)
		{
			if (target == i)
				target = a[i];
		}
	}
}
