<?php
$res = 0;
$A = fgets(STDIN);
$B = fgets(STDIN);
$C = fgets(STDIN);
$X = fgets(STDIN);

for ($a = 0; $a <= $A; ++$a)
    for ($b = 0; $b <= $B; ++$b)
        for ($c = 0; $c <= $C; ++$c)
            if (500*$a + 100*$b + 50*c == X)
                ++$res;
echo $res;
?>
