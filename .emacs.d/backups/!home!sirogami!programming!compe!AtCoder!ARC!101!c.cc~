#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main(int argc, char **argv)
{
	ll N, K; cin >> N >> K;
	vector<ll> x(N); for (ll i = 0; i < N; ++i) cin >> x[i];

	int res{1<<20};
	for (ll i = 0; i < n-k+1; ++i)
	{
		for (ll )
	}
}
