#include <iostream>
#include <queue>
using namespace std;

int main(int argc, char **argv)
{
	priority_queue<int> que;

	que.push(3);
	que.push(2);
	que.push(4);

	while (!que.empty())
	{
		std::cout << que.top() << std::endl;
		que.pop();
	}
}
