#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main(int argc, char **argv)
{
	ll N, K; cin >> N >> K; for (ll i = 0; i < N; ++i) ll a, cin >> a;

	ll res{1};

	for (ll i = K; i < N; i+=(K-1)) ++res;
	std::cout << res << std::endl;
}
