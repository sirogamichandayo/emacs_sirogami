#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main(int argc, char **argv)
{
	ll n, k, q;
	cin >> n >> k >> q;

	vector<ll> v(q);
	for (int i = 0; i < q; ++i) cin >> v[i];

	
}
